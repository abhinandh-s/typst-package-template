[package]
name = "package-name"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Name <name@example.com>"]
license = "MIT"
description = "A short description."
repository = "https://github.com/abhinandh-s/typst-package-template"
keywords = []
categories = []
exclude = ["example.*"]
