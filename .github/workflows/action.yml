steps:
  - uses: actions/checkout@v4

  # Use the local action (or your-org/your-repo@v1 if stored in its own repository)
  - uses: ./
  
  - name: Test import
    run: |
      typst compile - << 'EOF'
      #import "@local/mypkg:0.1.0": add
      #assert(add(1, 2) == 3)
      EOF
